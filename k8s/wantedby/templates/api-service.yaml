apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: api
  ports:
    - port: 80
      targetPort: {{ .Values.api.port }}
      protocol: TCP
